# Write your MySQL query statement below
select arg1.teacher_id, count(*) as cnt from (select teacher_id, count(subject_id) as cnt from Teacher group by teacher_id, subject_id) as arg1 group by teacher_id
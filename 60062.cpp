#include <string>
#include <vector>

using namespace std;

struct node
{
    int val;
    node *next;
    node(int val): val(val) {};
};


int solution(int n, vector<int> weak, vector<int> dist) {

    int answer = 0;
    return answer;
}
#include <stdio.h>
#include <vector>

class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int ans = 0;
        while (gain[i];i<gain.len();i++);


    }
};
int main()
{
    Solution solu;
    solu largestAltitude()
    



}